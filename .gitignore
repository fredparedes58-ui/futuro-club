# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*

# Test videos (large files)
public/test-video-*.mp4
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

dev-dist
.claude/*
!.claude/rules/

# Test coverage reports
coverage/

# Playwright output (reportes y artefactos de e2e — no versionar)
test-results/
playwright-report/
/blob-report/
/playwright/.cache/

# ONNX models (too large for Git — downloaded at runtime)
*.onnx

# ONNX Runtime WASM files (copied from node_modules by postinstall)
public/ort-wasm-*.wasm
public/ort-wasm-*.mjs

# Environment variables (NEVER commit these)
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
scripts/data/kaggle/
